{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Configure CMake",
            "type": "shell",
            "command": "cmake -G \"Unix Makefiles\" . -B build -DBUILD_TESTS=ON",
            "icon": { "id": "tools" },
        },
        {
            "label": "Compile Unit Tests",
            "type": "shell",
            "command": "cmake --build build --verbose",
            "icon": { "id": "gear" },
        }
    ]
}